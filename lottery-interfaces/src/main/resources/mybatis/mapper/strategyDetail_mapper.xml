<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lottery.infrastructure.dao.IStrategyDetailDao">
    <update id="deductStock">
        update strategy_detail
        set award_surplus_count = strategy_detail.award_surplus_count - 1
        where strategy_id = #{strategyId} and award_id = #{awardId}
        and award_surplus_count > 0
    </update>

    <select id="queryStrategyDetailList" resultType="lottery.infrastructure.po.StrategyDetail">
        select id, strategy_id, award_id, award_count,
                award_surplus_count,
               award_rate, create_time, update_time
        from strategy_detail
        where strategy_id = #{strategyId}
    </select>
    <select id="queryNoStockStrategyAwardList" resultType="java.lang.String" parameterType="java.lang.Long">
        select award_id
        from strategy_detail
        where strategy_id = #{strategyId} and award_surplus_count = 0
    </select>

</mapper>